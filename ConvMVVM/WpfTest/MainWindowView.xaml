<Window x:Class="WpfTest.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTest"
        mc:Ignorable="d"
        Height="450" Width="800"
        xmlns:behavior="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:convMVVM="https://github.com/gellston/ConvMVVM"
        convMVVM:DialogServiceViews.IsRegistered="True"
        convMVVM:ViewModelLocator.AutoWireViewModel="True">
    <behavior:Interaction.Behaviors>
        <convMVVM:EventToCommand EventName="Loaded" Command="{Binding LoadedCommand}"></convMVVM:EventToCommand>
    </behavior:Interaction.Behaviors>
  
    <DockPanel>
        <UniformGrid Height="40"
                     Rows="1"
                     DockPanel.Dock="Bottom">
            <Button Command="{Binding RunCommand}"></Button>
        </UniformGrid>
        <ContentControl convMVVM:RegionManager.RegionName="MainContent"></ContentControl>
    </DockPanel>
</Window>
